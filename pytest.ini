[pytest]
# Only the real unit/integration modules are collected. live_test.py,
# soak_test.py and stress_test.py are standalone manual scripts — run them
# directly (see their docstrings), not via pytest.
testpaths = tests
python_files = test_*.py
